{
  "name": "@dispatcher/controller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "dispatcher": "./dist/cli.js" },
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run test",
    "dev": "tsx src/cli.ts",
    "dispatcher": "tsx src/cli.ts"
  },
  "dependencies": {
    "@dispatcher/config": "workspace:*",
    "@dispatcher/domain": "workspace:*",
    "@dispatcher/observability": "workspace:*",
    "@dispatcher/persistence": "workspace:*",
    "@dispatcher/runner": "workspace:*",
    "@dispatcher/web": "workspace:*",
    "@fastify/static": "10.1.3",
    "fastify": "5.12.4"
  }
}
