{
  "name": "@dispatcher/runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": { ".": { "types": "./src/index.ts", "default": "./dist/index.js" } },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run test"
  },
  "dependencies": {
    "@dispatcher/domain": "workspace:*",
    "@dispatcher/protocol": "workspace:*"
  },
  "devDependencies": {
    "@dispatcher/adapters": "workspace:*",
    "@dispatcher/workspace": "workspace:*"
  }
}
